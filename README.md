# QwenAgent

Agent CLI pour diagnostiquer et corriger automatiquement des problèmes dans tes solutions .NET / web, en utilisant Qwen (via Ollama ou autre backend compatible) + Azure + git.

## Installation

```powershell
# Depuis un terminal PowerShell
git clone https://github.com/SaintSiboire/QwenAgent.git
cd QwenAgent\installer
.\install.ps1
